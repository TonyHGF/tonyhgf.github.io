# miccai-web-demo

Demostration of MICCAI topic modeling embedding results over years

Run Python's built-in server:

```bash
python -m http.server 8000
```

Open your browser to `http://localhost:8000`.
